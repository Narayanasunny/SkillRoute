<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/langContainer"
    android:orientation="vertical"
    android:layout_marginTop="12dp"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/tvLangToggle"
        android:text="▸ Hindi"
        android:textSize="16sp"
        android:textStyle="bold"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <LinearLayout
        android:id="@+id/langContent"
        android:orientation="vertical"
        android:visibility="gone"
        android:layout_marginTop="8dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView android:text="• CodeWithHarry    OPEN" style="@style/ListItem" />
        <TextView android:text="• Apna College     OPEN" style="@style/ListItem" />
        <TextView android:text="• WsCubeTech       OPEN" style="@style/ListItem" />
    </LinearLayout>
</LinearLayout>
